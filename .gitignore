# Growth-skill working state — Netlify publishes the repo root, so this must never deploy
growth/

# Environment variables — never commit real credentials
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Netlify
.netlify/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
