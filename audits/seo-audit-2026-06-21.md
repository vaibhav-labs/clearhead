# SEO Audit — Clearhead — 2026-06-21

## Summary

The site is in strong SEO health. Across all 26 indexable pages, titles and meta
descriptions are within length range, every page has exactly one H1 and a self-referencing
canonical, structured data is comprehensive and parses cleanly, no images exceed 500 KB, no
render-blocking scripts sit in `<head>`, and no external links are missing `rel="noopener"`.
The live homepage matches the local source (title, canonical, description, OG) — no
deployed-vs-local divergence. Only three Low-severity tidiness items remain; nothing
Critical, High, or Medium. Rules were re-checked: `RULES_LAST_VERIFIED` is 2026-06-15 (6 days
ago, inside the 30-day window), so no rule refresh was needed this run.

## Critical (0)

None.

## High (0)

None.

## Medium (0)

None. (The automated script flagged four sitemap `lastmod` entries as "older than last
commit," but on inspection the triggering commit — `9bbccee`, "SEO fixes" — only made 2-line
FAQ-schema edits to those posts, not content changes, and refreshed the sitemap in the same
commit. Per the rubric, days-stale `lastmod` on a non-content edit is Low, not Medium —
downgraded below.)

## Low (3)

- **Sitemap `lastmod` trails last commit by 1–6 days on four posts** — `sitemap.xml` — the
  06-15 "SEO fixes" commit touched these files (FAQ-schema alignment, ~2 lines each) after
  the listed `lastmod`. Changes were cosmetic, not content, so impact is negligible. Optional
  fix: bump these `<lastmod>` to `2026-06-15`.
  - `post-quiet-cracking.html` — lastmod `2026-06-11`, commit `2026-06-15`
  - `post-cant-switch-off.html` — lastmod `2026-06-13`, commit `2026-06-15`
  - `post-lonely.html` — lastmod `2026-06-09`, commit `2026-06-15`
  - `still-on-your-list.html` — lastmod `2026-06-14`, commit `2026-06-15`
- **Missing `twitter:card` meta** — `privacy-policy.html`, `terms.html` — both have complete
  OG tags (X/Twitter falls back to OG, so previews still render), but the explicit
  `twitter:card`/`twitter:*` set present on every other page is absent here. Low-impact
  consistency gap. Fix: add `<meta name="twitter:card" content="summary_large_image">` plus
  matching `twitter:title`/`twitter:description` to both files. (These are legal pages —
  minimal sharing relevance.)
- **`VJ.jpg` `<img>` lacks `width`/`height` attributes** — `index.html` (About section) — the
  other four homepage images declare dimensions; this one doesn't, leaving a small
  layout-shift (CLS) risk while the 1200×849 portrait loads. Fix: add `width="1200"
  height="849"` to the `<img src="VJ.jpg">` tag.

## What's already strong

- **Titles & descriptions** — all 26 pages within range (titles 28–60 chars, descriptions
  84–162 chars); no duplicates, no missing tags on indexable pages.
- **Headings** — exactly one H1 per page, no skipped levels.
- **Canonicals** — every indexable page has a self-referencing canonical; `thank-you.html` is
  correctly `noindex,nofollow` and disallowed in `robots.txt` (so its missing canonical/OG is
  intentional, not a defect).
- **Structured data** — rich and valid: homepage carries `LocalBusiness`/`ProfessionalService`
  with NAP, `GeoCoordinates`, `priceRange`, plus `Person`, `Organization`, `FAQPage`; posts
  carry `Article` + `BreadcrumbList` + `FAQPage`; tools carry `SoftwareApplication`; inner
  pages have `BreadcrumbList`. All JSON-LD parses.
- **Images** — every local image is under 500 KB (largest ≈ 265 KB); remote hero images pin
  width via query params; all `<img>` tags carry meaningful `alt` text.
- **Performance signals** — no render-blocking `<script src>` in `<head>`.
- **Links** — no external `target="_blank"` link is missing `rel="noopener noreferrer"`; no
  orphan indexable pages.
- **Deployed = local** — live `clearhead.in` homepage title, canonical, description, and OG
  all match the local `index.html`.
- **FAQ schema** — note that Google deprecated FAQ rich results (2026-05-07), so FAQ schema is
  no longer a rich-result lever; the site's `FAQPage` markup remains valid Schema.org and
  matches visible content, which is correct to keep.

## Next-action recommendation

Nothing here is urgent — this is a clean audit. If you want to close out the three Low items
in one pass (highest tidiness-per-effort first):

1. Add `width="1200" height="849"` to the `VJ.jpg` image in `index.html` (smallest, real CLS
   benefit).
2. Bump the four stale `<lastmod>` dates in `sitemap.xml` to `2026-06-15`.
3. Add `twitter:card` + `twitter:title`/`twitter:description` to `privacy-policy.html` and
   `terms.html` for cross-page consistency.

The bigger lever for this domain isn't on-page — it's authority and crawl demand (backlinks,
internal linking, sustained unique content). On-page SEO is effectively maxed out.

Audit complete. Reply with which items to fix.
