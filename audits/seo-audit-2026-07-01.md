# SEO Audit — Clearhead — 2026-07-01

## Summary
The site is in strong SEO health: all 26 indexable pages have valid titles, descriptions, canonicals, one H1 each, complete OG/Twitter cards, and valid structured data, with no deployed-vs-local divergence on the homepage. Only three Low-severity items surfaced — a stale homepage sitemap `lastmod`, one skipped heading level, and a missing `streetAddress` in the LocalBusiness schema. Rules were re-verified against the skill's `RULES_LAST_VERIFIED: 2026-06-15` (16 days old, inside the 30-day window, so no rule refresh was required this run); FAQ rich-results remain deprecated, so FAQ schema/visible mismatches stay Low.

## Critical (0)
None.

## High (0)
None.

## Medium (0)
None.

## Low (3)
- **Homepage sitemap `lastmod` is stale** — `sitemap.xml` (`https://clearhead.in/`) lists `2026-06-09`, but `index.html` was last changed `2026-06-21` (~12 days behind) — update the homepage `<lastmod>` to `2026-06-21`. *(Note: the automated `seo_audit.py` missed this because it derives the filename `clearhead.in` instead of `index.html` for the root URL and then skips the file — see "Skill rule updates".)*
- **Skipped heading level** — `still-on-your-list.html:191` — static markup jumps from `<h1>` (line 123) straight to `<h3>` with no intervening `<h2>` (the `<h2>` only appears later via JavaScript-injected quiz results); change the line-191 `<h3>If sitting with this with someone would be useful</h3>` to `<h2>` (or add a section `<h2>`).
- **LocalBusiness schema missing `streetAddress`** — `index.html:50` and `index.html:86` — the `PostalAddress` has `addressLocality`, `postalCode`, and `addressCountry` but no `streetAddress`; add `"streetAddress": "Lokhandwala Complex"` to complete NAP for local SEO. Likely intentional if there is no walk-in office, in which case no action is needed.

## What's already strong
- **Meta tags:** every indexable page has a title in the 30–60 char range and a description in the 120–160 char range; exactly one self-referencing canonical each; OG + Twitter cards complete, with `og:image:width/height/alt` present on the homepage.
- **Structured data:** `BreadcrumbList` on every inner page; `Article` schema complete (headline, datePublished, author, image, publisher) on all 16 blog posts; `LocalBusiness` on the homepage with `geo`, `hasMap`, `openingHours`, `priceRange`, and `telephone`; `SoftwareApplication` on the tool pages. All JSON-LD parses as valid JSON; no FAQ schema-vs-visible mismatches.
- **Headings:** exactly one H1 on every page; no skipped levels anywhere except the single case above.
- **Internal linking:** no orphan posts — all 16 posts are linked from `blog.html`, and all 5 tools are linked from `tools.html`.
- **Images:** all local images are under 500 KB (largest is `coaching-grad.jpg` at 271 KB); remote Unsplash images declare `width`/`height` attributes plus `loading="lazy"`, so no layout-shift (CLS) risk.
- **Sitemap & robots:** all 26 indexable pages are present in `sitemap.xml`; the `noindex`/disallowed pages (`thank-you.html`, `quiz.html`) are correctly excluded from both the sitemap and crawling; `robots.txt` is clean and points to the sitemap.
- **Deployed vs local:** the live homepage matches local source on title, canonical, description, and OpenGraph — no divergence.

## Next-action recommendation
1. Update the homepage `<lastmod>` in `sitemap.xml` from `2026-06-09` to `2026-06-21` (30-second fix; the only finding with any crawl-signal value).
2. Change the `still-on-your-list.html:191` heading from `<h3>` to `<h2>` to remove the skipped level.
3. Decide whether to add `streetAddress` to the homepage `LocalBusiness` schema (only if you want the physical address publicly in structured data).

All three are Low severity — none block indexing or meaningfully affect rankings, so this is genuinely optional polish rather than urgent work.

## Skill rule updates
No SEO *rule* changes this run (rules verified 2026-06-15, inside the 30-day window). However, this run confirmed **gaps and one bug in `scripts/seo_audit.py`** that are worth folding into the skill so the daily run catches them automatically:

1. **Homepage `lastmod` bug (causes a missed finding):** the sitemap-freshness loop maps the root URL `https://clearhead.in/` to filename `clearhead.in` (via `loc.rstrip("/").split("/")[-1]`), which does not exist on disk, so the homepage `lastmod` is never checked. It should fall back to `index.html`. This is exactly why the stale homepage `lastmod` above was invisible to the script.
2. **Checks the script does not perform** (all done manually this run): `BreadcrumbList` presence on inner pages, `LocalBusiness` NAP/geo/hasMap completeness, skipped-heading-level detection (it only counts H1s), orphan-page detection, and sitemap-completeness (every indexable page present).

A ready-to-paste enhanced `seo_audit.py` implementing all of the above has been saved alongside this report at `audits/seo_audit_enhanced.py`. Because the installed skill is a read-only cache in this session, it can't be updated from here — fold the enhanced script into the skill via **Settings → Capabilities**.

Audit complete. Reply with which items to fix.
