# SEO Audit — Clearhead — 2026-07-06

## Summary
On-page SEO remains clean — the automated script reports **0 findings across 33 pages** (titles, descriptions, canonicals, H1s, OG/Twitter, JSON-LD validity, alt text, image sizes, noopener). But this run found one Critical infrastructure problem: **the Netlify deploy is stale**. The live site is serving the repo as of commit `cb38458` (2026-06-15), while `origin/main` sits at `6ff0890` (pushed 2026-06-21) — three pushed commits, including the 06-15 SEO fixes, a full blog post, and `llms.txt`, have never reached production. Rules were re-verified against `RULES_LAST_VERIFIED: 2026-06-15` (21 days old, inside the 30-day window — no rule refresh required); FAQ rich results remain deprecated.

## Critical (1)

- **Netlify deploy is stale — live site is 3 pushed commits behind GitHub** — live serves `cb38458` (2026-06-15); `origin/main` = `6ff0890` (2026-06-21). Evidence (each independently conclusive): `https://clearhead.in/llms.txt` → **404** (added in `6ff0890`); `https://clearhead.in/post-ai-work-family-exhaustion.html` → **404** (added in `b62275b`, on origin); live `sitemap.xml` is **byte-identical** to `cb38458:sitemap.xml`; live homepage JSON-LD is missing `priceRange` (added in `9bbccee`); live `still-on-your-list.html` title is the pre-trim 69-char version. Consequences: the 2026-06-15 SEO fixes (FAQ schema alignment, title/desc trims, OG image dims, `priceRange`, sitemap lastmods) that previous audits marked "fixed" are **still not live**, and the daily site monitor's auto-fix pushes are not reaching production. Fix: open the Netlify dashboard → Deploys for clearhead.in, check for a failed/paused build around 2026-06-15–21 (or a disconnected GitHub webhook), and trigger "Deploy site" from `origin/main`. *(No live page links to a 404 and the live sitemap is internally consistent, so there's no active indexing damage — but nothing shipped since June 15 either.)*

## High (0)
None.

## Medium (0)
None.

## Low (4)

- **Content backlog grew — 6 finished posts exist locally but not on origin** — `post-rest-burnout.html` is committed (`acce436`, 2026-06-22) but **not pushed** (branch is ahead 1); `post-money-anxiety.html` + `post-ai-job-fear.html` are staged (with `blog.html` and `sitemap.xml`); `post-holding-it-together.html`, `post-impostor-syndrome.html`, `post-monday-dread.html` are untracked. The local `sitemap.xml` already lists all of them — **7 local-sitemap URLs currently 404 live**. Fix: after the Netlify deploy is repaired, commit the untracked/staged posts together with `blog.html` and `sitemap.xml` and push as one unit so the live sitemap never references a 404. All new posts are SEO-complete (one H1, canonical, Article + BreadcrumbList schema, description in range).
- **Homepage sitemap `lastmod` stale (carried over)** — `sitemap.xml:5` — homepage listed as `2026-06-09`, but `index.html` last changed `2026-06-21`. Update to the real date when the backlog ships.
- **Skipped heading level (carried over)** — `still-on-your-list.html:191` — `<h1>` (line 123) jumps to `<h3>` (line 191); the only `<h2>` is JS-injected (line 560). Change line 191 to `<h2>`.
- **LocalBusiness missing `streetAddress` (carried over)** — `index.html` — `PostalAddress` has locality/postcode/country but no `streetAddress`. Likely intentional (no public walk-in address); add only if one exists.

## What's already strong
- **Automated checks: 0 findings across 33 pages** — every indexable page has an in-range title and description, one self-referencing canonical, exactly one H1, complete OG + Twitter cards, valid JSON-LD, meaningful alt text on every image, and no external `_blank` link missing `rel="noopener"`.
- **Structured data:** `BreadcrumbList` on all inner pages, complete `Article` schema (incl. `dateModified`) on every post, `LocalBusiness` with NAP + `geo` + `hasMap` + `openingHours` + `priceRange`, `SoftwareApplication` on tools.
- **Sitemap hygiene:** all 29 indexable pages present, the 4 noindexed utility pages correctly excluded, no orphan pages.
- **Images:** no local image over 500 KB; remote Unsplash images declare width/height with `loading="lazy"` — no CLS risk.
- **Live-vs-local head tags on the homepage match exactly** (title, canonical, description, OG) — the staleness shows only in newer files and schema additions.

## Next-action recommendation
1. **Fix the Netlify deploy** (Critical) — check the Deploys log / GitHub webhook and redeploy from `origin/main`. Until this is fixed, every other repair is invisible, including three weeks of monitor pushes.
2. **Then ship the content backlog atomically** — push `acce436`, plus the 5 staged/untracked posts with `blog.html` and `sitemap.xml` in one commit (and refresh the homepage `lastmod` in the same change). Six finished posts are earning nothing while they sit local.
3. Optional tidy-up: `still-on-your-list.html:191` `<h3>`→`<h2>`.

## Skill rule updates
No SEO rule changes (verified 2026-06-15, within 30-day window). Proposed `scripts/seo_audit.py` / monitor improvements (skill file is read-only this session):
1. **Homepage `lastmod` bug (carried over):** the sitemap loop maps `https://clearhead.in/` to filename `clearhead.in` and silently skips it — fall back to `index.html` for the root URL.
2. **Deploy-freshness probe (new — would have caught today's Critical automatically):** fetch one sentinel file that changes with every push (`sitemap.xml` or `llms.txt`) and compare against `origin/main`'s copy; flag Critical if live content matches an older commit. The daily site monitor passed for 3 weeks because stale-but-valid pages all return 200.
3. **Sitemap-lists-a-404 cross-check (carried over):** verify every local `sitemap.xml` `<loc>` maps to a tracked, committed file.

Audit complete. Reply with which items to fix.
