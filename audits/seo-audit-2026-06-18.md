# SEO Audit — Clearhead — 2026-06-18

## Summary
The site is in strong technical-SEO health: titles and descriptions are all in range, every indexable page has a self-referencing canonical and exactly one H1, structured data (LocalBusiness, BreadcrumbList, Article) is complete and valid, and the live homepage matches local source on every key tag (no deployed-vs-local divergence). The only issue is cosmetic: four sitemap `lastmod` dates trail the actual last-modified date by 1–6 days. Rules were re-verified against `RULES_LAST_VERIFIED: 2026-06-15` (3 days old) — no rule refresh needed this run, and FAQ rich-results remain deprecated as of 2026-05-07.

## Critical (0)
None.

## High (0)
None.

## Medium (0)
None. (The automated script flagged the four sitemap items below as Medium; per the skill's rubric — Medium is "stale by weeks," Low is "a day stale" — these are 1–6 days stale on small edits, so they are downgraded to Low. Reasoning noted under each.)

## Low (4)
- **Sitemap `lastmod` trails real last-change date** — `sitemap.xml` (`post-lonely.html`) — bump `lastmod` from `2026-06-09` to `2026-06-15` (file edited in the 06-15 commit; 6 days stale).
- **Sitemap `lastmod` trails real last-change date** — `sitemap.xml` (`post-quiet-cracking.html`) — bump `lastmod` from `2026-06-11` to `2026-06-15` (4 days stale).
- **Sitemap `lastmod` trails real last-change date** — `sitemap.xml` (`post-cant-switch-off.html`) — bump `lastmod` from `2026-06-13` to `2026-06-15` (2 days stale).
- **Sitemap `lastmod` trails real last-change date** — `sitemap.xml` (`still-on-your-list.html`) — bump `lastmod` from `2026-06-14` to `2026-06-15` (1 day stale; this page had a 7-line title/desc/OG edit on 06-15).

Root cause: the 2026-06-15 commit (message: "…refresh sitemap lastmods…") edited these four pages but missed updating their corresponding `<lastmod>` entries. Low impact — `lastmod` is a crawl hint, not a ranking factor, and the lag is days, not weeks. Fixing it keeps the sitemap honest and nudges a recrawl of the changed pages.

## What's already strong
- **No deployed-vs-local divergence** — live `clearhead.in` homepage matches local `index.html` on `<title>`, canonical, meta description, and OG title/description/image.
- **Meta tags** — all pages within title (30–60) and description (120–160) length targets; exactly one canonical per indexable page; full OpenGraph + Twitter cards, with `og:image` carrying width/height/alt on the homepage.
- **Headings** — exactly one H1 per page; no skipped heading levels (checked post and tool pages).
- **Structured data** — `LocalBusiness` with complete NAP, `geo`, `hasMap`, `openingHours`, and `priceRange`; `BreadcrumbList` on all 21 inner pages; `Article` schema with required fields (headline, datePublished, author, image, publisher) on all 12 blog posts; all JSON-LD parses as valid JSON.
- **Internal linking** — no orphan pages: every blog post is linked from the blog hub and every tool from the tools hub; key pages (pricing, blog, tools) are linked site-wide.
- **Links** — no external `target="_blank"` links missing `rel="noopener"`.
- **Crawl hygiene** — `robots.txt` correctly disallows `thank-you.html`, `quiz.html`, `/api/`, and `/.netlify/`; sitemap lists 22 indexable pages and excludes the noindex/disallowed ones.
- **Images** — no local image exceeds the 500 KB threshold; remote hero images are width-pinned via query params.

## Next-action recommendation
1. Update the four `<lastmod>` dates in `sitemap.xml` to `2026-06-15` (one-line edits each) — the single concrete fix here.
2. Optional, and not a defect: post-only authority work. Two posts (`post-lonely-at-work`, `post-multitasking`) sit only in the blog index. Adding a contextual in-body link from a related, higher-traffic post would strengthen their internal-link signal — but this is enhancement, not a finding.

There are no Critical/High/Medium issues to prioritise; the foundation is solid.

## Skill rule updates
None. `RULES_LAST_VERIFIED` is 2026-06-15 (3 days ago), within the ~30-day window, so no rule research or Changelog edit was required this run.

Audit complete. Reply with which items to fix.
