# SEO Audit — Clearhead — 2026-07-03

## Summary
The live site remains in strong SEO health: all indexable pages have valid titles, descriptions, canonicals, one H1 each, complete OG/Twitter cards, valid structured data, no oversized images, and no external `_blank` links missing `rel="noopener"`. The homepage matches live on title/canonical/description/OG — no deployed-vs-local divergence on served pages. The only new item this run is a deploy-hygiene risk: two finished blog posts plus an updated `blog.html` and `sitemap.xml` are staged in git but not committed or deployed, and the *local* sitemap already lists two URLs that currently 404 live — so these must ship together. Rules were re-verified against the skill's `RULES_LAST_VERIFIED: 2026-06-15` (18 days old, inside the 30-day window, so no rule refresh was required); FAQ rich results remain deprecated, so FAQ schema/visible mismatches stay Low.

## Critical (0)
None. The **live** sitemap is clean — it does not yet list the two new posts, so Google is not being served any 404 URLs.

## High (0)
None.

## Medium (0)
None.

## Low (4)

- **Uncommitted posts + sitemap must deploy atomically (deploy-hygiene risk)** — `post-money-anxiety.html`, `post-ai-job-fear.html` (git status `A`, untracked→staged), `blog.html` (`M`), `sitemap.xml` (`M`) — the **local** `sitemap.xml` lists `https://clearhead.in/post-money-anxiety.html` (`lastmod 2026-07-01`) and `https://clearhead.in/post-ai-job-fear.html` (`lastmod 2026-06-23`), but both return **404 live**. The live sitemap does **not** list them yet, so there is no live defect right now. Fix: commit and deploy the posts, `blog.html`, and `sitemap.xml` in the **same** push, so the live sitemap never references a URL that 404s. Both posts are otherwise SEO-complete (1 H1, canonical, Article + BreadcrumbList schema, og:image, description 154–157 chars, clean heading hierarchy).

- **Homepage sitemap `lastmod` is stale (carried over)** — live `sitemap.xml` lists `https://clearhead.in/` as `2026-06-09`, but `index.html` last changed `2026-06-21` (~12 days behind) — update the homepage `<lastmod>` to `2026-06-21`. Outstanding since the 2026-07-01 report (unfixed because runs are report-only). *(The automated `seo_audit.py` still misses this: it maps the root URL to filename `clearhead.in` and skips it — see Skill rule updates.)*

- **Skipped heading level (carried over)** — `still-on-your-list.html:191` — markup jumps from `<h1>` (line 123) straight to `<h3>` with no intervening static `<h2>` (the `<h2>` only appears later via JS-injected quiz results at line 560). Change the line-191 `<h3>If sitting with this with someone would be useful</h3>` to `<h2>`. Outstanding since prior audits.

- **LocalBusiness schema missing `streetAddress` (carried over)** — `index.html:50` and `index.html:86` — `PostalAddress` has `addressLocality` ("Lokhandwala, Andheri West"), `postalCode`, and `addressCountry` but no `streetAddress`. Add `"streetAddress"` to complete NAP for local SEO, or leave as-is if there is no public walk-in address (likely intentional — no action needed then).

## What's already strong
- **Meta tags:** every indexable page has a title in range (28–60 chars; noindex utility pages aside, all core pages 39–60) and descriptions within 120–160 chars; one self-referencing canonical each; OG + Twitter cards complete with `og:image:width/height/alt` on the homepage.
- **Structured data:** `BreadcrumbList` on inner pages; `Article` schema complete on posts (including the two new ones); `LocalBusiness` on the homepage with `geo`, `hasMap`, `openingHours`, `priceRange`, `telephone`; `SoftwareApplication` on tools. All JSON-LD parses; no FAQ schema-vs-visible mismatches.
- **Headings:** exactly one H1 per page; no skipped levels except the single carried-over case above.
- **Images:** no local image exceeds 500 KB (largest ~271 KB); remote Unsplash images declare width/height + `loading="lazy"`, so no CLS risk.
- **Links:** no external `target="_blank"` link is missing `rel="noopener"`; no orphan posts.
- **Deployed vs local:** the live homepage matches local source on title, canonical, description, and OpenGraph — no divergence on any served page.
- **Automated script:** `scripts/seo_audit.py` reports 0 findings across 30 pages.

## Next-action recommendation
1. **When ready to publish**, commit and push `post-money-anxiety.html`, `post-ai-job-fear.html`, `blog.html`, and `sitemap.xml` together so the live sitemap and live pages stay in sync (no sitemap-listed 404s). *(Do NOT deploy on this scheduled run — this is the human's call.)*
2. In that same change, update the homepage `<lastmod>` in `sitemap.xml` from `2026-06-09` to `2026-06-21`.
3. Optionally fix the `still-on-your-list.html:191` `<h3>`→`<h2>` skip.

All findings are Low severity — nothing blocks indexing or serves wrong content on the live site today. The one item that could *become* a real problem (sitemap 404s) is avoided simply by deploying the staged files as a unit.

## Skill rule updates
No SEO *rule* changes this run (rules verified 2026-06-15, inside the 30-day window; FAQ rich-results deprecation still current). Two `scripts/seo_audit.py` gaps confirmed again — worth folding in so the daily run catches them automatically:
1. **Homepage `lastmod` bug:** the sitemap-freshness loop maps root URL `https://clearhead.in/` to filename `clearhead.in` and skips it, so the homepage `lastmod` is never checked (this is why the stale homepage `lastmod` above stays invisible to the script). Fall back to `index.html` for the root URL.
2. **Sitemap-lists-a-404 check (new, high value):** the script does not verify that every URL in `sitemap.xml` resolves (or that a sitemap-listed local file is actually tracked/committed). Adding a check that cross-references sitemap `<loc>` entries against tracked files (and/or live HTTP status) would have flagged the staged-but-undeployed posts automatically.

Audit complete. Reply with which items to fix.
