# SEO Audit — Clearhead — 2026-07-09

## Summary
The site is in excellent SEO health: 36 pages scanned, zero Critical/High/Medium findings, and the live homepage matches local source exactly (deploy pipeline current). Yesterday's audit-fix commit (2026-07-08) resolved the previous batch — descriptions, FAQ/schema alignment, headings, and sitemap lastmod all now check out. Rules were last verified 2026-06-15 (24 days ago, within the 30-day window), so no rule refresh was required this run; audit applied the current rule set including the FAQ rich-results deprecation.

## Critical (0)
None.

## High (0)
None.

## Medium (0)
None.

## Low (2)
- **robots.txt Disallow conflicts with noindex** — `robots.txt:3-4` — `/thank-you.html` and `/quiz.html` are both Disallowed and carry `noindex`. Disallow blocks crawling, so Google can never see the noindex; if either URL earns an external link it can surface as "Indexed, though blocked by robots.txt" with no snippet. Fix: delete the two Disallow lines and rely on the existing `noindex, nofollow` meta (keep `/api/` and `/.netlify/` Disallows).
- **Duplicate partial business entity in homepage schema** — `index.html:74` — the `ProfessionalService` node has only `name` (no address, geo, telephone, or `@id` link), while the separate `LocalBusiness` node is complete (NAP, geo, hasMap, priceRange, openingHours). Two unlinked business entities slightly dilute entity clarity. Fix: either merge ProfessionalService into the LocalBusiness node (as an additional `@type`) or cross-reference them via `@id`.

## What's already strong
- Every indexable page: title 30–60 chars, description 120–160 chars, exactly one canonical, one H1, no skipped heading levels, complete OG + Twitter cards with og:image width/height/alt.
- All JSON-LD parses; Article schema on all 21 posts with full required fields; BreadcrumbList on inner pages; SoftwareApplication on all 5 tool pages; FAQPage text matches visible content exactly.
- LocalBusiness with full NAP, geo, hasMap, priceRange, openingHours.
- Sitemap: 32 URLs, all lastmod dates match git history, noindex pages correctly excluded; robots.txt and sitemap both serve 200.
- No images over 500 KB (largest 268 KB), all images have meaningful alt text, no render-blocking scripts in `<head>`, all `target="_blank"` links carry `rel="noopener noreferrer"`, no orphan pages.
- Live homepage tags identical to local source — no deployed-vs-local divergence.

## Next-action recommendation
Both findings are Low; nothing is urgent. If touching the repo anyway: (1) remove the two page-level Disallow lines from robots.txt, (2) consolidate the ProfessionalService/LocalBusiness entities on index.html. No other action needed — the highest-impact SEO work now is off-page (backlinks/mentions), not on-page.

## Notes on this run
- The skill's automated script (`seo_audit.py`) reported 0 findings; independent manual verification confirmed the clean state and surfaced only the 2 Low items above, which are outside the script's check scope.
- Untracked local file `audits/site-health-2026-07-09.md` exists (today's site-monitor report) — not a deploy divergence, listed for awareness only.
- Report-only run: no files edited, no commits, no deploys.

Audit complete. Reply with which items to fix.
